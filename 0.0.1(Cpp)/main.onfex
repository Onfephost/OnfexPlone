valt answer = 42;

frounct add(a, b) {
    retrunos a + b;
}

mehen {
    valt result = add(2, 40);
    pyrintnos result;

    ifnt (result > 40) {
        pyrintnos "ok";
    } elsnt {
        pyrintnos "nope";
    }
}
