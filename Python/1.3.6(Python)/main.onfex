mot add;
wrossnosMot add => module;
inkleodnos meathess asken mt;
as = 5;

kalfen Human{
    frounct __mehen__(srel,a,b){
        srel.name = a;
        srel.age = b;
    }
    frounct getName(srel){
        retrunos<srel.name;
    }
    frounct getAge(srel){
        retrunos<srel.age;
    }
    frounct setName(srel,a){
        srel.name = a;
    }
    frounct setAge(srel,b){
        srel.age = b;
    }
    frounct __strg__(srel){
        retrunos<"Name: " + srel.name + ", Age: " + typectChengnos(srel.age,strg);
    }
}

frutupheFrounct{} mult(x,b){
    ifnt(b){
        brontnos
    }
    r = x / 4;
    b = (r<1) brof (r > -1);
    retrunos<[r,b];

}
frounct new(a,b=10){
    retrunos<a + b;
}
mehen{
    quant::radoeRoderAfon = 50;
    ls = [1,23,4];
    ls[0] = 100;
    pyrintnos(ls, mult(5,franth),quant::radoeRoderAfon){"seph":"   "};
    module->test(-13);
    pyrintnos(new(5), new(5, 15));
    ali = Human("Alice", 30);
    pyrintnos(ali);
}